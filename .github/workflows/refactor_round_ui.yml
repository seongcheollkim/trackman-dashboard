# Temporary trigger marker - main branch runner performs the patch.
name: Integrate round record UI

on:
  push:
    branches:
      - feature/round-record

permissions:
  contents: write

jobs:
  patch-round-ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Temporary branch workflow; main-branch runner performs the patch."
